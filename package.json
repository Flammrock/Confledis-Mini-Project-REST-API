{
  "name": "confledis-mini-project",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd server && npm run clean && npm run build && cd ../client && npm run build && move build ../server/build/client",
    "start": "cd server/build && node app.js",
    "install": "cd client && npm install && cd ../server && npm install"
  },
  "author": "Lemmy Briot",
  "license": "MIT"
}
